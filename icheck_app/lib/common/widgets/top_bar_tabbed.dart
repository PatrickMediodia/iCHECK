import 'package:flutter/material.dart';

class TopBarTabbed extends StatelessWidget {
  const TopBarTabbed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Implement Me");
  }
}
