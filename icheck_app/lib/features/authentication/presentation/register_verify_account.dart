import 'package:flutter/material.dart';

class RegisterVerifyAccount extends StatelessWidget {
  const RegisterVerifyAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Implement this page");
  }
}
