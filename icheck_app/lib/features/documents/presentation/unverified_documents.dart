import 'package:flutter/material.dart';

class UnverifiedDocuments extends StatelessWidget {
  const UnverifiedDocuments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Implement this page");
  }
}
