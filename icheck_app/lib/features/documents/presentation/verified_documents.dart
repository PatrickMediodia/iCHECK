import 'package:flutter/material.dart';

class VerifiedDocuments extends StatelessWidget {
  const VerifiedDocuments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Implement this page");
  }
}
