import 'package:flutter/material.dart';

class SharedFromOthers extends StatelessWidget {
  const SharedFromOthers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Implement this page");
  }
}
